import 'package:flutter/material.dart';

class MatchesPlaceholder extends StatelessWidget {
  const MatchesPlaceholder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('My Matches — Coming Soon')),
    );
  }
}
